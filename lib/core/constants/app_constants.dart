import 'package:flutter/material.dart';

class AppColors {
  // Dark Coffee Theme Palette
  static const Color primary = Color(0xFF6B3A2A);       // Nâu cà phê đậm
  static const Color primaryLight = Color(0xFF9E5A44);  // Nâu kem sáng
  static const Color accent = Color(0xFFD4A65A);        // Vàng caramel
  static const Color background = Color(0xFF140F0A);    // Đen cà phê ấm
  static const Color surface = Color(0xFF1C140E);       // Nâu tối
  static const Color surfaceVariant = Color(0xFF261D15); // Nâu card sáng hơn
  static const Color onPrimary = Color(0xFFFFF8F2);      // Trắng sữa kem
  static const Color onBackground = Color(0xFFE6DED7);   // Xám trắng
  static const Color onSurface = Color(0xFFE6DED7);      // Xám trắng
  
  // Status Colors
  static const Color success = Color(0xFF4CAF50);       // Đã hoàn thành / Đang mở
  static const Color error = Color(0xFFF44336);         // Lỗi / Đóng cửa
  static const Color warning = Color(0xFFFF9800);       // Đang pha chế
  static const Color info = Color(0xFF2196F3);          // Chờ duyệt
  
  // Neutral Colors
  static const Color greyDark = Color(0xFF555555);
  static const Color greyLight = Color(0xFFBBBBBB);
}

class AppSpacing {
  static const double xs = 4.0;
  static const double s = 8.0;
  static const double m = 16.0;
  static const double l = 24.0;
  static const double xl = 32.0;
}
